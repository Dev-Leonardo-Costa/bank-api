global:
  scrape_interval: 5s

scrape_configs:
  - job_name: 'bank-api'

    metrics_path: '/actuator/prometheus'

    scheme: https

    static_configs:
      - targets:
          - 'bank-api-92w8.onrender.com'